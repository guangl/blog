---
phase: 03-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lockb
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "bun run build 后 public/sitemap.xml 存在"
    - "sitemap.xml 包含博客文章 URL 条目"
    - "package.json 的 dependencies 中有 hexo-generator-sitemap"
  artifacts:
    - path: "package.json"
      provides: "hexo-generator-sitemap 依赖声明"
      contains: "hexo-generator-sitemap"
    - path: "public/sitemap.xml"
      provides: "构建输出的 sitemap 文件"
      contains: "<loc>"
  key_links:
    - from: "package.json dependencies.hexo-generator-sitemap"
      to: "public/sitemap.xml"
      via: "hexo generate 构建时插件自动注册并输出"
      pattern: "hexo-generator-sitemap"
---

<objective>
通过 bun add 安装 hexo-generator-sitemap 插件，使 bun run build 自动在 public/ 生成 sitemap.xml。

Purpose: 满足 SEO-02（sitemap.xml 自动生成），让搜索引擎能发现博客所有页面 URL。
Output: package.json 新增 hexo-generator-sitemap 依赖，bun.lockb 更新，构建后产出 public/sitemap.xml。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-seo/03-CONTEXT.md
@.planning/phases/03-seo/03-RESEARCH.md

<interfaces>
<!-- 来自 RESEARCH.md Package Legitimacy Audit -->

hexo-generator-sitemap 审核结论：
- Registry: npm (hexojs 官方组织)
- 版本: 3.0.1
- 创建: 2012 年 (13+ 年历史)
- 最后更新: 2026-05-15
- postinstall script: 无
- Disposition: Approved

安装命令（per D-08，使用 bun 作为包管理器）：
  bun add hexo-generator-sitemap

默认行为（per D-09）：
- 安装后无需修改 _config.yml
- hexo generate 自动输出 public/sitemap.xml
- 默认包含文章、分类、标签页面的 URL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 安装 hexo-generator-sitemap 插件（per D-08）</name>
  <files>package.json, bun.lockb</files>
  <read_first>
    - package.json — 确认当前依赖列表，验证 hexo-generator-sitemap 尚未安装
    - .planning/phases/01-仓库基础/01-CONTEXT.md — 确认 bun 为当前包管理器（bun add 命令前提）
    - .planning/phases/03-seo/03-RESEARCH.md §Standard Stack 和 §Package Legitimacy Audit — 插件版本和审核结论
  </read_first>
  <action>
    执行以下安装命令（per D-08，bun 为包管理器）：
      bun add hexo-generator-sitemap

    安装前提（执行前确认）：
    - bun.lockb 存在（确认 Phase 1 已完成 bun 迁移）
    - pnpm-lock.yaml 不存在（确认迁移完成）
    - 若 bun.lockb 不存在，停止执行并报告：「Phase 1 bun 迁移尚未完成，请先执行 Phase 1 和 Phase 2」

    安装后验证（per D-09）：
    - 不需要修改 _config.yml（默认配置已满足需求）
    - 不需要在 _config.yml 添加 sitemap: 配置块（插件安装即自动注册）
  </action>
  <verify>
    <automated>grep "hexo-generator-sitemap" /Users/guang/Projects/blog/package.json && echo "PASS: dependency added" || echo "FAIL: dependency not found"</automated>
  </verify>
  <acceptance_criteria>
    - package.json 的 dependencies 对象包含键 `hexo-generator-sitemap`（值为版本字符串）
    - bun.lockb 文件存在且为最新（bun add 后自动更新）
    - _config.yml 未被修改（per D-09，无需额外配置）
  </acceptance_criteria>
  <done>hexo-generator-sitemap 出现在 package.json dependencies，bun.lockb 已更新</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 构建验证 sitemap.xml 生成</name>
  <files></files>
  <read_first>
    - .planning/phases/03-seo/03-RESEARCH.md §Validation Architecture — 验证命令规范
  </read_first>
  <action>
    执行构建并验证 sitemap 输出：
      bun run build

    验证以下条件（per D-09 默认输出路径）：
    1. public/sitemap.xml 文件存在
    2. sitemap.xml 包含 `<loc>` 标签（说明有实际 URL 条目）
    3. sitemap.xml 包含博客 URL（应包含 _config.yml 中配置的 url 字段值）

    构建失败时的处理：
    - 若构建报错与 hexo-generator-sitemap 无关（如 Phase 2 配置问题），记录错误信息到 SUMMARY.md 的 blockers 字段，不要自行修改 Phase 2 的文件
    - 若 sitemap.xml 不存在但构建成功，检查 package.json 中插件是否正确安装
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -5 && test -f /Users/guang/Projects/blog/public/sitemap.xml && grep -c "<loc>" /Users/guang/Projects/blog/public/sitemap.xml | awk '{if($1>0) print "PASS: sitemap has " $1 " URLs"; else print "FAIL: sitemap has no URLs"}'</automated>
  </verify>
  <acceptance_criteria>
    - bun run build 退出码为 0（构建成功，无 error 级别输出）
    - public/sitemap.xml 文件存在（`test -f public/sitemap.xml` 返回真）
    - public/sitemap.xml 包含至少 1 个 `<loc>` 标签（`grep -c "<loc>" public/sitemap.xml` 输出 >= 1）
  </acceptance_criteria>
  <done>bun run build 成功，public/sitemap.xml 存在且含有效 URL 条目</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 本地安装 | bun add 从 npm 拉取 hexo-generator-sitemap 包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | bun add hexo-generator-sitemap | mitigate | 已完成 Package Legitimacy Audit（RESEARCH.md）：hexojs 官方组织，13+ 年历史，无 postinstall script，Disposition: Approved |
| T-03-03 | Information Disclosure | public/sitemap.xml | accept | sitemap 内容为公开博客 URL，设计上即为公开；不含任何私密路径 |
| T-03-04 | Denial of Service | 构建时 sitemap 超大 | accept | 博客文章数量极少（v1.0 阶段），sitemap 体积不构成威胁 |

**Security summary:** hexo-generator-sitemap 经过手动审核（替代 slopcheck），风险可接受。sitemap.xml 输出内容为设计上公开的 URL 列表，无安全问题。
</threat_model>

<verification>
Phase 3 Plan 02 完整验证：

```bash
# 确认插件已安装
grep "hexo-generator-sitemap" package.json

# 构建并验证 sitemap
bun run build
test -f public/sitemap.xml && echo "PASS: sitemap.xml exists" || echo "FAIL"
grep -c "<loc>" public/sitemap.xml
```
</verification>

<success_criteria>
1. package.json dependencies 包含 hexo-generator-sitemap（per D-08）
2. _config.yml 未被修改（per D-09，无需额外 sitemap 配置）
3. bun run build 成功（退出码 0）
4. public/sitemap.xml 存在（`test -f public/sitemap.xml` 为真）
5. sitemap.xml 包含至少 1 个 `<loc>` 条目
</success_criteria>

<output>
当所有 task 完成后，创建 .planning/phases/03-seo/03-02-SUMMARY.md
</output>

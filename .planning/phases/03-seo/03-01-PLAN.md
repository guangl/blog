---
phase: 03-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/_posts/rust-ownership-intro.md
  - source/_posts/thinking-about-learning.md
  - _config.stellar.yml
autonomous: true
requirements:
  - CONT-03
  - SEO-01

must_haves:
  truths:
    - "至少一篇文章出现在「技术教程」分类页面（/categories/技术教程/）"
    - "至少一篇文章出现在「个人思考」分类页面（/categories/个人思考/）"
    - "每篇文章构建后的 HTML head 中包含 og:title 和 og:description 标签"
    - "_config.stellar.yml 不包含覆盖 open_graph 为 false 的配置"
  artifacts:
    - path: "source/_posts/rust-ownership-intro.md"
      provides: "技术教程分类示例文章，front-matter 含 category: 技术教程"
      contains: "category: 技术教程"
    - path: "source/_posts/thinking-about-learning.md"
      provides: "个人思考分类示例文章，front-matter 含 category: 个人思考"
      contains: "category: 个人思考"
    - path: "_config.stellar.yml"
      provides: "确认 open_graph 不被覆盖为 false"
  key_links:
    - from: "source/_posts/rust-ownership-intro.md"
      to: "public/categories/技术教程/"
      via: "hexo-generator-category 在构建时读取 front-matter"
      pattern: "category: 技术教程"
    - from: "node_modules/hexo-theme-stellar/layout/_partial/head.ejs"
      to: "og:title / og:description"
      via: "open_graph(og_args()) 调用，从 page.title 和正文前 200 字自动提取"
      pattern: "open_graph\\.enable"
---

<objective>
创建两篇分类示例文章（技术教程 + 个人思考），并确认 Stellar 主题的 Open Graph 配置不被覆盖为 false。

Purpose: 满足 CONT-03（分类有示例文章）和 SEO-01（文章页有 og:title、og:description），为搜索引擎爬取提供真实可预览的内容。
Output: 两个 Markdown 文章文件，以及确认 _config.stellar.yml 不干扰 OG 配置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-seo/03-CONTEXT.md
@.planning/phases/03-seo/03-RESEARCH.md

<interfaces>
<!-- 文章 front-matter 格式（来自 RESEARCH.md Pattern 1 和 hello-world.md 示例） -->

已有示例 source/_posts/hello-world.md（无 category 字段，Phase 3 新文章需要加 category）：
```yaml
---
title: Hello World
---
```

Phase 3 文章 front-matter 目标格式：
```yaml
---
title: <文章标题>
date: 2026-05-25 10:00:00
category: 技术教程        # 或 个人思考 — 必须与 Phase 2 分类名完全一致
tags:
  - <标签1>
---
```

Stellar head.ejs open_graph 触发逻辑（已验证源码第 130-132 行）：
```javascript
if (theme.open_graph && theme.open_graph.enable) {
  <%- open_graph(og_args()) %>
}
```

Hexo open_graph helper og:description 来源优先级（已验证 open_graph.js）：
1. page.description（front-matter 中的 description 字段）
2. page.excerpt（<!-- more --> 分隔符前的内容）
3. page.content 前 200 字（自动截断 + strip HTML）

D-06 决策：不在 front-matter 写 description，依赖第 3 条自动提取。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建「技术教程」分类示例文章</name>
  <files>source/_posts/rust-ownership-intro.md</files>
  <read_first>
    - source/_posts/hello-world.md — 了解现有文章 front-matter 格式和目录位置
    - .planning/phases/02-主题与内容结构/02-CONTEXT.md — 确认分类名「技术教程」的精确中文字符（D-04）
    - .planning/phases/03-seo/03-RESEARCH.md §Pattern 1 — front-matter 格式规范，category 单数字段
  </read_first>
  <action>
    在 source/_posts/ 创建文件 rust-ownership-intro.md，front-matter 包含以下字段：
    - title: Rust 所有权机制：从零理解内存安全
    - date: 2026-05-25 10:00:00
    - category: 技术教程（必须与 Phase 2 D-04 分类名完全一致，区分全半角）
    - tags: [Rust, 内存管理]

    正文要求（per D-01、D-06）：
    - 3–5 段落，总字数 200 字以上（保证 og:description 自动提取有内容）
    - 内容主题：Rust 所有权（Ownership）概念介绍，示例性质无需深度
    - 不在 front-matter 写 description 字段（per D-06，由 Hexo helper 自动提取）
    - 不使用 <!-- more --> 标记（依赖正文前 200 字自动截断）

    正文结构建议：
    1. 首段：所有权概念简介（约 60 字）
    2. 第二段：三条核心规则（约 80 字）
    3. 第三段：借用与引用简介（约 60 字）
    4. 第四段：实践意义总结（约 40 字）
  </action>
  <verify>
    <automated>grep -c "category: 技术教程" /Users/guang/Projects/blog/source/_posts/rust-ownership-intro.md && wc -m /Users/guang/Projects/blog/source/_posts/rust-ownership-intro.md | awk '{if($1>300) print "PASS: content length ok"; else print "FAIL: content too short"}'</automated>
  </verify>
  <acceptance_criteria>
    - source/_posts/rust-ownership-intro.md 存在
    - 文件包含 `category: 技术教程`（精确字符）
    - 文件包含 `title: Rust 所有权机制：从零理解内存安全`
    - 文件不包含 `description:` 字段（per D-06）
    - 文件字符数 > 300（保证正文有实质内容支撑 og:description 提取）
  </acceptance_criteria>
  <done>rust-ownership-intro.md 存在，front-matter 含正确 category，正文至少 3 段（共 200 字以上）</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建「个人思考」分类示例文章</name>
  <files>source/_posts/thinking-about-learning.md</files>
  <read_first>
    - source/_posts/rust-ownership-intro.md — 参考刚创建的文章 front-matter 格式保持一致
    - .planning/phases/02-主题与内容结构/02-CONTEXT.md — 确认分类名「个人思考」的精确中文字符（D-04）
    - .planning/phases/03-seo/03-RESEARCH.md §Pattern 1 — front-matter 格式规范
  </read_first>
  <action>
    在 source/_posts/ 创建文件 thinking-about-learning.md，front-matter 包含以下字段：
    - title: 关于持续学习的几点思考
    - date: 2026-05-25 11:00:00
    - category: 个人思考（必须与 Phase 2 D-04 分类名完全一致，区分全半角）
    - tags: [学习, 成长]

    正文要求（per D-01、D-06）：
    - 3–5 段落，总字数 200 字以上（保证 og:description 自动提取有内容）
    - 内容主题：关于技术学习方法和持续成长的个人思考，示例性质无需深度
    - 不在 front-matter 写 description 字段（per D-06）
    - 不使用 <!-- more --> 标记

    正文结构建议：
    1. 首段：学习在技术领域的重要性（约 60 字）
    2. 第二段：主动学习 vs 被动学习的区别（约 80 字）
    3. 第三段：记录与输出的价值（约 60 字）
    4. 第四段：写博客作为学习工具（约 40 字）
  </action>
  <verify>
    <automated>grep -c "category: 个人思考" /Users/guang/Projects/blog/source/_posts/thinking-about-learning.md && wc -m /Users/guang/Projects/blog/source/_posts/thinking-about-learning.md | awk '{if($1>300) print "PASS: content length ok"; else print "FAIL: content too short"}'</automated>
  </verify>
  <acceptance_criteria>
    - source/_posts/thinking-about-learning.md 存在
    - 文件包含 `category: 个人思考`（精确字符）
    - 文件包含 `title: 关于持续学习的几点思考`
    - 文件不包含 `description:` 字段（per D-06）
    - 文件字符数 > 300
  </acceptance_criteria>
  <done>thinking-about-learning.md 存在，front-matter 含正确 category，正文至少 3 段（共 200 字以上）</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 确认 _config.stellar.yml 未覆盖 open_graph 为 false（per D-07）</name>
  <files>_config.stellar.yml</files>
  <read_first>
    - _config.stellar.yml — 查看当前内容（当前为空，Phase 2 执行后可能已写入配置）
    - node_modules/hexo-theme-stellar/_config.yml — 确认 open_graph 默认值为 enable: true（第 23-25 行附近）
    - .planning/phases/03-seo/03-RESEARCH.md §Pattern 2 和 §Pitfall 4 — OG 配置规则和危险写法
  </read_first>
  <action>
    检查 _config.stellar.yml 的当前内容：

    情况 A：文件为空或不包含任何 `open_graph` 相关行
    - 无需操作，Stellar 默认值 open_graph.enable: true 自动生效
    - 不要写入任何 open_graph 配置（per Pitfall 4：写空块反而可能导致 enable 为 undefined）

    情况 B：文件包含 `open_graph:` 块但没有 `enable: true`
    - 删除该 open_graph 块，让默认值生效

    情况 C：文件包含 `open_graph:\n  enable: false`
    - 将该行修改为 `  enable: true`

    情况 D：文件包含 `open_graph:\n  enable: true`
    - 无需操作，配置正确

    关键约束（per D-07 和 Pitfall 4）：
    - 不要写入 `open_graph:` 块而不包含 `enable: true`（会导致 enable 为 undefined）
    - 最终状态：文件要么不含 open_graph 配置（依赖默认值），要么含 `open_graph:\n  enable: true`
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
content = open('_config.stellar.yml').read()
# Check: if open_graph block exists, it must have enable: true; or no open_graph block at all
has_og = 'open_graph' in content
if not has_og:
    print('PASS: no open_graph override, default enable:true applies')
    sys.exit(0)
if re.search(r'open_graph.*enable.*true', content, re.DOTALL):
    print('PASS: open_graph.enable: true present')
    sys.exit(0)
print('FAIL: open_graph block exists but enable:true not found')
sys.exit(1)
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - _config.stellar.yml 要么不包含 `open_graph` 关键字（依赖默认值），要么包含 `enable: true`
    - _config.stellar.yml 不包含 `open_graph.enable: false` 或 `enable: false` 在 open_graph 块内
    - 构建后文章 HTML 包含 og:title 标签：`bun run build && grep -l "og:title" public/2026/05/25/rust-ownership-intro/index.html` 输出文件路径（非空）
  </acceptance_criteria>
  <done>_config.stellar.yml 的 open_graph 配置状态确认：Stellar 默认 enable: true 生效，构建后文章 HTML head 包含 og:title 和 og:description</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者 → 静态文件 | 文章内容由开发者自己编写，无外部用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | source/_posts/*.md | accept | 文章由开发者本人写入，无外部输入路径，无 XSS 风险；Hexo open_graph helper 内置 escapeHTML（已验证源码第 73 行） |
| T-03-02 | Information Disclosure | og:description 自动提取 | accept | 描述来自文章公开正文，设计上即为公开内容，无敏感信息泄露风险 |
| T-03-SC | Tampering | npm/bun 安装 | N/A（本 Plan 无 bun add 操作） | N/A — sitemap 插件安装在 Plan 02 处理 |

**Security summary:** Phase 3 Plan 01 仅操作静态 Markdown 文件和 YAML 配置，无网络请求、用户输入或认证逻辑。ASVS L1 无适用检查项。
</threat_model>

<verification>
构建验证（Plan 01 完成后运行）：

```bash
# 确认文章存在且 category 正确
grep "category: 技术教程" source/_posts/rust-ownership-intro.md
grep "category: 个人思考" source/_posts/thinking-about-learning.md

# 构建并验证分类页和 OG 标签
bun run build
ls public/categories/技术教程/
ls public/categories/个人思考/
grep "og:title" public/2026/05/25/rust-ownership-intro/index.html
grep "og:description" public/2026/05/25/rust-ownership-intro/index.html
```
</verification>

<success_criteria>
1. source/_posts/rust-ownership-intro.md 存在，front-matter 含 `category: 技术教程`，正文 200 字以上
2. source/_posts/thinking-about-learning.md 存在，front-matter 含 `category: 个人思考`，正文 200 字以上
3. 两篇文章 front-matter 均不含 `description:` 字段（og:description 自动提取，per D-06）
4. _config.stellar.yml 不含 `open_graph.enable: false`（Stellar 默认 enable: true 生效）
5. bun run build 成功，public/2026/05/25/rust-ownership-intro/index.html 包含 og:title 和 og:description
</success_criteria>

<output>
当所有 task 完成后，创建 .planning/phases/03-seo/03-01-SUMMARY.md
</output>
